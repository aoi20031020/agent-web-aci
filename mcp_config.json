{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--browser=msedge",
        "--cdp-endpoint", "http://localhost:9222"
      ]
    }
  }
}
